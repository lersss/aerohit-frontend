meta:
  environment: node
  toolchain:
    name: npm
    version: 20
build:
  additionalCommands:
    - npm install
    - npm run build
run:
  command: npx serve -s build -l 3000
  containerPort: 3000
  persistenceMount: /data